{
  "name": "notify4",
  "version": "0.0.1",
  "description": "Notify4 - Full stack app with Python backend and React frontend",
  "scripts": {
    "dev": "concurrently \"npm run api\" \"npm run frontend\"",
    "api": "cd api && python index.py",
    "frontend": "cd frontend && npm install && npm run dev",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd api && pip install -r requirements.txt && python index.py"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
